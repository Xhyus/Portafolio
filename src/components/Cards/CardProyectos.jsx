import React from 'react'
import { HStack, VStack, Heading, WrapItem, Wrap, Text, Label } from '@chakra-ui/react'

const CardProyectos = ({ imagen, descripcion, titulo, tecnologias }) => {
    return (
        <>

        </>
    )
}

export default CardProyectos